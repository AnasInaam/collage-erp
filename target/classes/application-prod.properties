# Production Profile Configuration for Supabase PostgreSQL
# Database Configuration
spring.datasource.url=jdbc:postgresql://db.ogjutpejtgtaydlqsuez.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=FbR53okymq5Wo7A7
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Configuration for Production
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000
spring.datasource.hikari.leak-detection-threshold=60000

# JPA Configuration for PostgreSQL Production
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Production JWT secret (use environment variable for security)
app.jwt.secret=${JWT_SECRET:changeMeInProduction}

# Production logging
logging.level.com.example.collegeerp=INFO
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN

# Production server configuration
server.port=${PORT:8080}
server.forward-headers-strategy=framework
