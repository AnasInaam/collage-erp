[phases.setup]
nixPkgs = ['openjdk21', 'maven']

[phases.build]
cmds = ['mvn clean package -DskipTests -Dmaven.test.skip=true']

[phases.start]
cmd = 'java -Dserver.port=8080 -Dspring.profiles.active=prod -Djava.awt.headless=true -jar target/college-erp-1.0.0.jar'